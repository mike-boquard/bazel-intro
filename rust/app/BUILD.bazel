load("@rules_rust//rust:defs.bzl", "rust_binary")

rust_binary(
    name = "app",
    srcs = ["src/main.rs"],
    edition = "2021",
    deps = [
        "//rust/greeter",
        "@crates//:colored",
    ],
)
